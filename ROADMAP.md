# 演进路线图（ROADMAP）

> 记录 2026-09-17 关于「怎么更智能」的讨论成果，作为后续开发的依据。
> 当前已完成的能力见 README 的功能表和开发路线，本文只写「下一步」。

## 📐 「智能」的五个维度（现状评估）

| 维度 | 现状 | 提升手段 | 优先级 |
|------|------|---------|--------|
| ① 知道得多 | 已有 get_weather + web_search | 加数据源收益递减 | 低 |
| ② 做得对（任务执行） | 工具箱模式已就绪（AGENT_TOOLS） | 垂直深耕具体业务点 | 高 |
| ③ 记得住 | 仅 6 轮对话上下文，重启失忆 | 对话存储 + 画像 + 错题本 | **最高** |
| ④ 会主动 | 纯被动问答 | 早晨简报、提醒、定时播报 | 高（音箱灵魂） |
| ⑤ 答得快 | 首音 ~4s（流式已做） | 本地 TTS、流式 ASR | 中 |

## 🧠 近期：记忆层（下一步首选）

### 第 1 层：对话日志（存原始记录）

- SQLite 表 `conversations`：角色、内容、时间、用了什么工具、耗时
- 收益：重启不失忆（当前内存 history 重启即丢）、历史可回溯
- 选型：SQLite（零部署、单文件、数据不出家门，符合本地隐私路线）

### 第 2 层：喜好画像（提炼长期记忆）★ 感知"它懂我"的关键

- 表 `memories`：家在杭州 / 通勤坐地铁 / 不吃香菜 / 喜欢周杰伦 …
- 每轮对话**完成后异步**提炼：「这轮透露了什么长期信息？有则存、重复则合并」
- 下轮对话注入 system prompt（只挑相关条目，几十字，便宜且准）
- 效果示例：
  ```
  「今天穿什么」→ "今天杭州28度，你通勤坐地铁的话穿轻便点，晚上降温带件外套"
                    ↑ 来自画像                    ↑ 来自实时天气
  ```

### 反馈错题本（经验记忆，不是蒸馏）

```
提问 → 回答 → 用户反馈"不对，应该是…"
  → 存「问题 + 修正后好答案」到经验表
  → 下次相似问题先检索（相似度匹配）
  → 注入 prompt："上次用户纠正过：应该这样答"
  → 越用越合口味
```

- 生效快（纠正一次下次就变）、零训练成本、可删除
- 需要反馈入口：语音说"不对，应该是…"，或测试页加 👎 按钮
- 本质：RAG + 反馈记忆（检索式经验增强）

## 🌅 中期：主动播报（从"你问我答"到"它来找你"）

- 早晨简报：定时（APScheduler）触发 = 天气 + 新闻 + 今日提醒 一次播报
- 提醒/闹钟：「明天 8 点提醒我吃药」→ 存提醒表 → 到点主动 TTS 播报
- LED 屏同步显示提醒内容（见下节协议）
- 工具循环架构已能支撑，缺的只是定时触发 + 内容模板

## 🖥️ 中期：LED 显示协议（并入原路线图第 4 步）

- **电子时钟是第一个常驻帧**：后端加 `GET /api/time`（或 WS 定时推时间帧），
  ESP32 LED 屏直接显示后端下发的时:分
  → 设备**不需要 RTC 芯片也不需要 NTP**，时间以后端为准，永远不漂
- 时间来源：服务端每轮对话已实时取 `datetime.now()` 注入 prompt（非写死），
  同一时间源复用给 LED
- 协议演进：时钟帧 → 天气图标帧 → 对话表情帧 → 提醒推送帧

## 🎯 中期：垂直业务点（选 1~2 个做深）

| 业务点 | 价值 | 依赖 |
|--------|------|------|
| 提醒/闹钟/日程 | 零外部依赖、天天用、最能体现"记性" | SQLite + 定时器 |
| 出行场景包 | 天气 + 穿衣 + 附近景点 + 路线 一句话编排 | 高德 Key（已注册一半，暂缓） |
| 做饭助手 | "冰箱里有鸡蛋西红柿能做什么" | 菜谱数据 + 记忆层 |

- 原则：**只选 1~2 个做深**，其他保持通用；每个业务点建一组标准测试语句做回归评测
  （如：「你好呀」→直接回答 /「今天天气」→get_weather /「红烧肉怎么做」→web_search）
- 高德接入时已有调研结论：个人认证后 POI/路线每日免费额度数千次，足够

## 🧪 远期：蒸馏专属小模型（数据攒够再说）

- 前提：错题本/对话日志积累数千条高质量个人问答后才有意义
- 做法：用积累数据把 qwen3:8b 的对话风格+工具习惯蒸馏到 1.7b 级别
- 收益：后端可搬到树莓派级机器，整机成本再降一档；回复风格完全个人化
- 原则：**先跑错题本攒数据，再考虑训练**——个人对话量现阶段喂不饱训练管线

## 🗺️ 合并后的里程碑

```
✅ 已完成：语音 MVP → 流式 TTS → 全链路流式 → 天气/搜索工具 Agent
⬜ 近期  ：对话存储（SQLite）→ 反馈错题本 → 画像提炼
⬜ 中期  ：主动播报（简报/提醒）→ LED 时钟与显示协议 → 垂直场景包（高德）
⬜ 远期  ：蒸馏专属小模型 → 树莓派级低成本部署
```
